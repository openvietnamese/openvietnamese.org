{
  "name": "openvietnamese-org",
  "version": "1.0.0",
  "description": "Open Vietnamese — openvietnamese.org monorepo",
  "scripts": {
    "dev":   "npx @11ty/eleventy --input=src --output=_site --serve",
    "build": "npx @11ty/eleventy --input=src --output=_site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
